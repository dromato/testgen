<project name="TestGenerator" basedir="." default="run">
	<property name="dir.src" value="src" />
	<property name="dir.build" value="build" />
	<property name="dir.classes" value="${dir.build}/classes" />
	<property name="dir.jar" value="${dir.build}/jar" />
	<property name="dir.lib" value="libs" />

	<property name="classes.main" value="testgen.app.TestGenerator" />

	<path id="classpath">
		<fileset dir="${dir.lib}" includes="**/*.jar" />
	</path>

	<target name="clean">
		<delete dir="${dir.build}" />
	</target>

	<target name="compile" depends="clean">
		<mkdir dir="${dir.classes}" />
		<javac srcdir="${dir.src}" destdir="${dir.classes}" classpathref="classpath" />
	</target>

	<target name="jar" depends="compile">
		<mkdir dir="${dir.jar}" />
		<jar destfile="${dir.jar}/${ant.project.name}.jar" basedir="${dir.classes}">
			<manifest>
				<attribute name="Main-Class" value="${classes.main}" />
			</manifest>
		</jar>
	</target>

	<target name="wrap" depends="jar">

	</target>

	<target name="run" depends="wrap">

	</target>
</project>